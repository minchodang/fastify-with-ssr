{
  "name": "my-turbo-repo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --no-cache",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.0.0",
    "typescript": "^4.0.0",
    "eslint": "^7.0.0"
  },
  "packageManager": "pnpm@9.3.0"
}
